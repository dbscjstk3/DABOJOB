# FastAPI 기본
fastapi>=0.114.1
uvicorn[standard]==0.30.1

# Transformers for model (임시로 vLLM 대신)
transformers>=4.40.0
torch>=2.0.0

# Redis and common dependencies
redis==5.0.1
python-dotenv==1.0.0

# Text processing
regex==2024.5.15

# vLLM (추후 설치)
# vllm==0.6.2
